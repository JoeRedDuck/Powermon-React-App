[pytest]
markers =
    integration: marks tests as integration tests that require a running server (deselect with '-m "not integration"')

# Exclude dangerous production database tests by default
addopts = --ignore=tests/test_pg_machine_rename.py
